{
  "mcpServers": {
    "agentic-developer-mcp-docker": {
      "command": "uv",
      "args": [
        "uv run python -m src.mcp_server.server" 
      ],
      "description": "Agentic Developer MCP Server running in Docker via stdio"
    }
  }
}